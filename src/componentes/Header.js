import React from 'react';
import '../assets/estilos/Header.css';

const Header = () => (
  <header class="header">
    <button class="header__button">Home</button>
    <p class="header__logout"><a href="twitter.com">Cerrar Sesión</a></p>
  </header>
);

export default Header;
